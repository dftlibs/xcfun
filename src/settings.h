
enum xc_setting
  {
    XC_SETTING_SRLDA_ERF_MU = 0,
    XC_SETTING_LEN // Has to be last
  };

// Default values for settings
static double xc_settings[XC_SETTING_LEN] = 
  {
    0.5, // XC_SETTING_SRLDA_ERF_MU
  };

